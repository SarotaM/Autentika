import React from 'react';
import { StyledLoader, StyledContainer } from './Loader.styles';

const Loader = () => {
  return (
    <StyledContainer>
      <StyledLoader />
    </StyledContainer>
  );
};

export default Loader;
